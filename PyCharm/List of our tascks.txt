"""
This is a multi-line comment with docstrings

print("Hello world")
print("Hello universe")
print("Hello everyone")
"""

"""
Plan lekcji

* PyCharm IDE
* Interpreter Phytona - bezkompilacyjnie
* Podstawy
    - Interpratacja w konsoli przykłady dodawanie i tekst
    - Średniki ; - userfreandly
    - L1 - Nowy plik main.py - phyton file
        * Run plik działania liczbowe
        * Podpowiedzi interpretera [print()]
        * Działania na liczbach +-*/%
        Tutaj jesteśmy
        * Print teksty i liczby *

    Przeniesiono na Lekcje 2:
        * Komentarze # ; '3x"'
        * TODO
        * Zmienna 1 2 operator przypisania tekst print
        * Pobieranie zmienną imput() operator konkatenacji + " " +
    - L2 - Rozwinięcie
        * Kolejność działań + * ()
        * Typy zmienne - Float - zmiennoprzecinkowa
        * Dzielenie konwersja do int //
        * Mnożenie i potęgowanie **
        * Konwersja na str() i int()
        * CTRL + Shift

---------------------------------------------------------------------------------------------------------
---------------------------------------------------------------------------------------------------------
    - L2.Ćwiczenia - Pierwsze taski
        * Napisz program, który pobierze od użytkownika jego imię, nazwisko i wiek oraz poproś go o podanie dwóch liczb.
          2 liczby będą obliczane, a dodatkowo na końcu będzie wyświetlone słowo dziękuję tyle razy ile wartość liczby 2.

    - L2.Konwersja - Uzupełnienie zmiemmych i konwersje, skrócone zapisy
        * Przypisanie o wartość =+x ; += (inkrementacja i dekrementacja w innych programach)
        * Konwersja typów - automatycznie int; float x = input("tekst" int() + int()) Suma bez konwersji - Konkatenacja [5 + 5 = 55  //  5 + 5 = 10]
        * del

        * budowanie konkatenacji sktóconej na stringach
          print(f'Cześć {zmienna_imie}!}
          print(f'Mam {zmienna_wiek} lata.}

        * Zadanie dodatkowe, przerobienie pliku na sktóconą wersję

    - L3 - Instrukcje warunkowe
        * bull - True / False == (czy są sobie równe - operator porównania) 5 == 5 True != False
        * Różny != ; > , < , >= , <= ;
        * if 5==5: [if True: / if False:]
            print("Prawda")
        print("Koniec")
        vs
            print("Koniec")
        * else x > y:
        * else x <= y:
        * else x == 10:
        * elif:

    - L3.Ćwiczenia
        * Sklep - Jeśli pełnoletni, jakiej płci odczytując z imienia lub pobranie od użytkownika płci, to może kupić energydrinki / alkohol i papierosy
        * Ile musi poczekać
        * Ile masz lat, jeśli więcej lub 18 to może jeśli nie to nie może i brakuje mu tylu lat do tego a tylu do zostania prezydentem
        * Kategoria prawka B1 oraz zostać posłem 21, ławnik 30 lat.
        * Uprawnienia do emetytury 60 lat kobieta, 65 lat mężczyzna
        *

    - L4 - Operatory logiczne
        * if x > y:
            if v == z:
            print("vavv")
        VS
        AND, OR, NOT
        * if x > y and v == z:
            print("vavv")

        * if not
        * kolejność stopniowania operatorów OR vs AND
            if True or False and False:
                print("Prawda")
            else:
                print("Fausz")
            VS
            if (True or False) and False:
                print("Prawda")
            else:
                print("Fausz")
"""
